/**
 * Barrel export — re-exports FileUploadProgress from the components directory
 * so that `@/components/FileUploadProgress` resolves correctly.
 *
 * @module components/FileUploadProgress
 */

export { FileUploadProgress } from "../app/components/file-upload-progress";
export type { FileUploadProgressProps } from "../app/components/file-upload-progress";
